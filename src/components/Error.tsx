import styled from "styled-components";

const Error = () => {
  return <Wrapper>Please login first..</Wrapper>;
};

const Wrapper = styled.div`
  padding-top: 50vh;
  display: flex;
  justify-content: center;
`;

export default Error;
